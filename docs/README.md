# PriceMate Canada - GitHub Pages

This folder contains the static landing page for PriceMate Canada, deployed via GitHub Pages.

## 🌐 Live URL

**https://akaash-nigam.github.io/iOS_PriceMateCanada/**

## 📂 Contents

- `index.html` - Main landing page
- `styles.css` - Styling
- `script.js` - Interactive features
- `assets/` - Images and icons

## 🚀 Deployment

This site is automatically deployed via GitHub Pages whenever changes are pushed to the `docs/` folder on the `master` branch.

## 🔧 How to Update

1. Edit files in the `docs/` folder
2. Commit and push to GitHub:
   ```bash
   git add docs/
   git commit -m "Update landing page"
   git push origin master
   ```
3. Changes will be live within 1-2 minutes

## 💰 Cost

**FREE** - GitHub Pages is completely free for public repositories!

## ⚙️ Configuration

GitHub Pages is configured to:
- Source: `docs` folder on `master` branch
- Custom domain: (can be configured later)
- HTTPS: Enforced

---

**Previously deployed on**: Google Cloud Run (now retired to save costs)
**Current hosting**: GitHub Pages (free, fast, simple)
